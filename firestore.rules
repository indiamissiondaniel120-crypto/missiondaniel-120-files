rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /students/{studentId} {
      // For this educational prototype, we allow read/write access to the students collection
      // so the Admin Panel can manage registrations and the Login page can verify credentials.
      allow read, write: if true;
    }
  }
}